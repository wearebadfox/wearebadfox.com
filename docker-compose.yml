version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: wearebadfox/app
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    environment:
      - NODE_ENV=development
      - GATSBY_WEBPACK_PUBLICPATH=/
